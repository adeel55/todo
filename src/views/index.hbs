{{> header }}
    <h1>Hello, world!</h1>
    <ul class="list-unstyled">
        {{#each names}}
        <li>
            <div class="btn btn-success">{{ this }}</div>
        </li>
        {{/each}}
    </ul>
{{> footer }}