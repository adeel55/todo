<!doctype html>
<html lang="en">
    <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        *{
            font-family: Arial, Helvetica, sans-serif;
        }
        .heading{
            color: royalblue;
            text-align: center;
            
        }
        h5, h3,h4,h6{
            margin-bottom: 2px;
            
        }
        .container{
            display: flex;
            justify-content: center;
        }
        .inner-container{
            max-width: 350px;
            border: 1px solid #aaa;
            border-radius: 10px;
            padding: 2rem;
        }
        .description{
            font-size: small;
            color:dimgray;
            word-wrap: break-word;
        }
    </style>
    </head>
    <body>
        <div class="container">
            <div class="inner-container">
                <h3 class="heading"><u>Tasks To Be Finish By Today</u></h3>
                <h6 style="color: darkred; margin: 2px;text-align: center;">Deadlines: {{{ today }}}</h6>
                <div style="text-align: center;">
                    <ul style="list-style: none; padding-left: 0px;">
                        {{#each tasks}}
                        <li>
                            <h4>{{this.title}}</h4>
                            <div class="description">{{this.description}}</div>
                        </li>
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
    </body>
</html>